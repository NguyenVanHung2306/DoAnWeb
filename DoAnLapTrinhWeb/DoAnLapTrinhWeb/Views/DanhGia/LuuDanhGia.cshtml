@model DoAnLapTrinhWeb.Models.tbPhieuDanhGia
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<head>
    <link rel='stylesheet prefetch' href='https://netdna.bootstrapcdn.com/font-awesome/3.2.1/css/font-awesome.css'>
    <style>
        div.stars {
            width: 270px;
            display: inline-block;
        }

        input.star {
            display: none;
        }

        label.star {
            float: right;
            padding: 10px;
            font-size: 36px;
            color: #444;
            transition: all .2s;
        }

        input.star:checked ~ label.star:before {
            content: '\f005';
            color: #FD4;
            transition: all .25s;
        }

        input.star-5:checked ~ label.star:before {
            color: #FE7;
            text-shadow: 0 0 20px #952;
        }

        input.star-1:checked ~ label.star:before {
            color: #F62;
        }

        label.star:hover {
            transform: rotate(-15deg) scale(1.3);
        }

        label.star:before {
            content: '\f006';
            font-family: FontAwesome;
        }
    </style>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            var stars = document.querySelectorAll(".star");

            stars.forEach(star => {
                star.addEventListener('click', () => {
                    const rating = parseInt(star.getAttribute('data-diem'));
                    const textRating = document.getElementById('text-rating');
                    ratingButton.setAttribute("value", rating);
                });
            });
        });
    </script>
</head>
@{
    var sachId = ViewBag.sachId;
    var userId = ViewBag.userId;
    int a = 1;
    int b = 2;
    int c = 3;
    int d = 4;
    int e = 5;
}
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="LuuDanhGia">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                @* <label asp-for="sachId" class="control-label"></label> *@
                <input type="hidden" asp-for="sachId" class="control-label" value="@sachId" />
            </div>
            <div class="form-group">
                <label asp-for="diem" class="control-label"></label>
                <input asp-for="diem" class="control-label" />
                <span asp-validation-for="diem" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="binhluan" class="control-label"></label>
                <input asp-for="binhluan" class="form-control" />
                <span asp-validation-for="binhluan" class="text-danger"></span>
            </div>
            <div class="form-group">
                @* <label asp-for="userId" class="control-label"></label> *@
                <input type="hidden" asp-for="userId" class="control-label" value="@userId" />
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>


